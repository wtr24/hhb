---
phase: 03-equity-overview
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - backend/requirements.txt
  - backend/models/ohlcv.py
  - backend/models/fundamentals.py
  - backend/alembic/versions/0003_equity_overview.py
  - backend/api/routes/fx.py
  - backend/api/routes/equity.py
  - backend/api/main.py
  - backend/analysis/__init__.py
  - backend/analysis/black_scholes.py
  - backend/analysis/insider.py
  - backend/ingestion/sources/finnhub_source.py
  - backend/tests/api/__init__.py
  - backend/tests/api/test_equity.py
  - backend/tests/api/test_fx.py
  - backend/tests/analysis/__init__.py
  - backend/tests/analysis/test_black_scholes.py
  - backend/tests/analysis/test_insider.py
autonomous: true
requirements:
  - EQUITY-06
  - EQUITY-09
  - EQUITY-11
  - EQUITY-12

must_haves:
  truths:
    - "scipy is importable in the backend container"
    - "OHLCV model has an interval column with default '1d'"
    - "Fundamentals model has roe column"
    - "GET /api/fx/USD/GBP returns a rate from the fx_rates table"
    - "Black-Scholes Greeks function returns correct delta/gamma/vega/theta for known inputs"
    - "Insider clustering filter excludes transactionCode F (10b5-1)"
    - "Test scaffold files exist for all Phase 3 backend features"
  artifacts:
    - path: "backend/requirements.txt"
      provides: "scipy dependency"
      contains: "scipy>=1.11.0"
    - path: "backend/models/ohlcv.py"
      provides: "OHLCV model with interval column"
      contains: "interval"
    - path: "backend/api/routes/fx.py"
      provides: "FX rate REST endpoint"
      exports: ["router"]
    - path: "backend/analysis/black_scholes.py"
      provides: "Black-Scholes Greeks calculator"
      exports: ["bs_greeks"]
    - path: "backend/analysis/insider.py"
      provides: "Insider transaction clustering logic"
      exports: ["cluster_insiders"]
    - path: "backend/tests/api/test_equity.py"
      provides: "Test scaffold for equity endpoints"
      min_lines: 50
    - path: "backend/tests/analysis/test_black_scholes.py"
      provides: "Test scaffold for Black-Scholes math"
      min_lines: 30
  key_links:
    - from: "backend/api/routes/fx.py"
      to: "backend/models/fx_rate.py"
      via: "async SQLAlchemy query"
      pattern: "FXRate"
    - from: "backend/api/main.py"
      to: "backend/api/routes/fx.py"
      via: "include_router"
      pattern: "fx_router"
---

<objective>
Wave 0 foundation: add scipy dependency, OHLCV interval column, Fundamentals ROE column, FX endpoint (deferred from Phase 2), Black-Scholes + insider analysis modules, Finnhub source helper, and full test scaffold for Phase 3.

Purpose: Every subsequent wave depends on these foundational additions. The FX endpoint closes the EQUITY-11 gap. The test scaffold enables TDD-style verification throughout execution.
Output: Updated requirements.txt, new migration, FX route, analysis modules, test files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-equity-overview/03-CONTEXT.md
@.planning/phases/03-equity-overview/03-RESEARCH.md
@.planning/phases/03-equity-overview/03-VALIDATION.md

@backend/requirements.txt
@backend/models/ohlcv.py
@backend/models/fundamentals.py
@backend/models/fx_rate.py
@backend/api/main.py
@backend/api/routes/quote.py
@backend/api/database.py
@backend/ingestion/config.py
@backend/tests/ingestion/conftest.py

<interfaces>
<!-- Existing interfaces the executor needs -->

From backend/models/ohlcv.py:
```python
class OHLCV(Base):
    __tablename__ = "ohlcv"
    time = Column(TIMESTAMP(timezone=True), primary_key=True, nullable=False)
    ticker = Column(String(20), primary_key=True, nullable=False)
    open = Column(Numeric(18, 6))
    high = Column(Numeric(18, 6))
    low = Column(Numeric(18, 6))
    close = Column(Numeric(18, 6))
    volume = Column(BigInteger())
    source = Column(String(20))
```

From backend/models/fundamentals.py:
```python
class Fundamentals(Base):
    __tablename__ = "fundamentals"
    time = Column(TIMESTAMP(timezone=True), primary_key=True, nullable=False)
    ticker = Column(String(20), primary_key=True, nullable=False)
    pe_ratio = Column(Numeric(10, 4))
    ev_ebitda = Column(Numeric(10, 4))
    market_cap = Column(BigInteger())
    debt_equity = Column(Numeric(10, 4))
    source = Column(String(20))
```

From backend/models/fx_rate.py:
```python
class FXRate(Base):
    __tablename__ = "fx_rates"
    # time, base (String(3)), quote (String(3)), rate (Numeric), source
```

From backend/api/main.py:
```python
# Router registration pattern:
app.include_router(quote_router)
app.include_router(macro_router)
app.include_router(ingest_router)
```

From backend/api/routes/quote.py:
```python
router = APIRouter(prefix="/api", tags=["quotes"])
# Uses: from ..database import get_async_db
# Uses: from ..redis_client import get_redis
# Uses: from cache.ttl import cache_get, cache_set
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dependencies + migration + analysis modules</name>
  <files>
    backend/requirements.txt,
    backend/models/ohlcv.py,
    backend/models/fundamentals.py,
    backend/alembic/versions/0003_equity_overview.py,
    backend/analysis/__init__.py,
    backend/analysis/black_scholes.py,
    backend/analysis/insider.py,
    backend/ingestion/sources/finnhub_source.py
  </files>
  <read_first>
    backend/requirements.txt,
    backend/models/ohlcv.py,
    backend/models/fundamentals.py,
    backend/alembic/versions/0002_ingestion_hypertables.py,
    backend/api/routes/quote.py,
    .planning/phases/03-equity-overview/03-RESEARCH.md (lines 360-410 for BS implementation),
    .planning/phases/03-equity-overview/03-CONTEXT.md
  </read_first>
  <action>
1. **requirements.txt**: Append `scipy>=1.11.0` and `finnhub-python>=2.4.0` after the existing `pytest-asyncio` line. Do NOT modify any existing lines.

2. **backend/models/ohlcv.py**: Add `interval = Column(String(5), default='1d', nullable=False)` after the `source` column. Update `__table_args__` to change the existing index from `("ticker", "time")` to `("ticker", "time", "interval")` and update the PK conceptually — the migration handles the actual DDL. The composite PK becomes `(time, ticker, interval)`.

3. **backend/models/fundamentals.py**: Add `roe = Column(Numeric(10, 4))` after `debt_equity`. This column stores Return on Equity from yfinance `info["returnOnEquity"]`.

4. **Alembic migration `0003_equity_overview.py`**: Create migration that:
   - Adds `interval VARCHAR(5) DEFAULT '1d' NOT NULL` to `ohlcv` table
   - Adds `roe NUMERIC(10,4)` to `fundamentals` table
   - Drops old PK on ohlcv, creates new composite PK on `(time, ticker, interval)`
   - Drops old index `ix_ohlcv_ticker_time`, creates new index on `(ticker, time, interval)`
   - Use `op.execute()` for PK changes since Alembic doesn't natively support composite PK modification on TimescaleDB
   - Downgrade reverses all changes
   - Follow existing migration pattern from 0001/0002 (import from alembic import op; import sqlalchemy as sa)

5. **backend/analysis/__init__.py**: Empty file.

6. **backend/analysis/black_scholes.py**: Implement `bs_greeks(S, K, T, r, sigma, option_type='call') -> dict` exactly as documented in 03-RESEARCH.md lines 368-404. Function returns `{'price', 'delta', 'gamma', 'vega', 'theta'}`. Guard against T <= 0 or sigma <= 0 returning all None. Add `iv_percentile_rank(current_iv: float, iv_history: list[float]) -> float` that returns percentile 0-100.

7. **backend/analysis/insider.py**: Implement `cluster_insiders(transactions: list[dict], window_days: int = 14) -> dict`:
   - Input: list of Finnhub insider transaction dicts with keys `name`, `share`, `change`, `filingDate`, `transactionDate`, `transactionCode`, `transactionPrice`, `isDerivative`
   - Filter OUT `transactionCode` in `("F", "A", "D")` — these are 10b5-1/tax/award/disposition (per EQUITY-08 and Research)
   - Keep only `transactionCode` in `("P", "S")` — purchases and sales
   - Group remaining transactions into clusters: transactions within `window_days` of each other
   - Return `{"buy_count": int, "sell_count": int, "buy_sell_ratio": float|None, "clusters": [{"start_date", "end_date", "transactions": [...]}], "multi_insider": bool}` where `multi_insider` = True if 2+ distinct `name` values bought within same cluster window

8. **backend/ingestion/sources/finnhub_source.py**: Create helper functions that call Finnhub REST API:
   - `fetch_short_interest(symbol: str, api_key: str) -> dict|None` — GET `https://finnhub.io/api/v1/stock/short-interest?symbol={symbol}&token={api_key}`, return latest entry or None
   - `fetch_insider_transactions(symbol: str, api_key: str) -> list[dict]` — GET `https://finnhub.io/api/v1/stock/insider-transactions?symbol={symbol}&token={api_key}`, return `data` array
   - `fetch_company_news(symbol: str, api_key: str, from_date: str, to_date: str) -> list[dict]` — GET `https://finnhub.io/api/v1/company-news?symbol={symbol}&from={from_date}&to={to_date}&token={api_key}`
   - All functions use `requests.get()` with 10s timeout, return empty list/None on HTTP error, log errors
   - Import `from cache.rate_limiter import check_rate_limit` and `from api.redis_client import redis_client` — check `check_rate_limit(redis_client, "finnhub")` before each call
  </action>
  <verify>
    <automated>cd C:/hhbfin && python -c "from analysis.black_scholes import bs_greeks, iv_percentile_rank; r=bs_greeks(100,100,0.25,0.05,0.2,'call'); assert r['delta'] is not None; assert 0 < r['delta'] < 1; print('BS OK')" && python -c "from analysis.insider import cluster_insiders; r=cluster_insiders([{'name':'A','transactionCode':'P','transactionDate':'2026-01-01','share':100,'change':100,'filingDate':'2026-01-02','transactionPrice':50,'isDerivative':False},{'name':'B','transactionCode':'F','transactionDate':'2026-01-01','share':50,'change':50,'filingDate':'2026-01-02','transactionPrice':50,'isDerivative':False}]); assert r['buy_count']==1; assert r['sell_count']==0; print('Insider OK')"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "scipy>=1.11.0" backend/requirements.txt` returns 1
    - `grep -c "finnhub-python>=2.4.0" backend/requirements.txt` returns 1
    - `grep -c "interval" backend/models/ohlcv.py` returns at least 1
    - `grep -c "roe" backend/models/fundamentals.py` returns at least 1
    - `python -c "from analysis.black_scholes import bs_greeks"` succeeds
    - `python -c "from analysis.insider import cluster_insiders"` succeeds
    - `ls backend/alembic/versions/0003_equity_overview.py` succeeds
    - `ls backend/ingestion/sources/finnhub_source.py` succeeds
    - `grep "transactionCode.*F" backend/analysis/insider.py` shows F is filtered
  </acceptance_criteria>
  <done>scipy and finnhub-python in requirements.txt; OHLCV has interval column; Fundamentals has roe column; migration 0003 created; black_scholes.py returns correct Greeks for known inputs; insider.py filters 10b5-1 and clusters within window; finnhub_source.py has 3 REST helper functions.</done>
</task>

<task type="auto">
  <name>Task 2: FX endpoint + equity routes stub + test scaffold</name>
  <files>
    backend/api/routes/fx.py,
    backend/api/routes/equity.py,
    backend/api/main.py,
    backend/tests/api/__init__.py,
    backend/tests/api/test_equity.py,
    backend/tests/api/test_fx.py,
    backend/tests/analysis/__init__.py,
    backend/tests/analysis/test_black_scholes.py,
    backend/tests/analysis/test_insider.py
  </files>
  <read_first>
    backend/api/routes/quote.py,
    backend/api/main.py,
    backend/api/database.py,
    backend/api/redis_client.py,
    backend/models/fx_rate.py,
    backend/tests/ingestion/conftest.py,
    .planning/phases/03-equity-overview/03-VALIDATION.md,
    .planning/phases/03-equity-overview/03-RESEARCH.md (lines 128-164 for Finnhub REST schema)
  </read_first>
  <action>
1. **backend/api/routes/fx.py**: Create FX rate endpoint to close the Phase 2 deferral (D-17 in 02-CONTEXT.md):
   ```python
   router = APIRouter(prefix="/api", tags=["fx"])

   @router.get("/fx/{base}/{quote}")
   async def get_fx_rate(base: str, quote: str, db: AsyncSession = Depends(get_async_db)):
   ```
   - Query `FXRate` table for the given base/quote pair, ordered by time desc, limit 1
   - Check Redis cache first via `cache_get(redis_client, f"fx:{base.upper()}{quote.upper()}")`, return cached if hit
   - If DB hit, return `{"base": base, "quote": quote, "rate": float(row.rate), "timestamp": row.time.isoformat(), "stale": True}`
   - If no data, return 404 `{"error": "fx_rate_not_found"}`
   - Follow same import pattern as quote.py: `from ..database import get_async_db`, `from ..redis_client import get_redis`, `from cache.ttl import cache_get, cache_set`, `from models.fx_rate import FXRate`

2. **backend/api/routes/equity.py**: Create stub route file for Phase 3 equity-specific endpoints. These will be implemented in later waves but the router must exist now for test scaffold:
   ```python
   router = APIRouter(prefix="/api/equity", tags=["equity"])

   @router.get("/earnings/{ticker}")  # EQUITY-04
   @router.get("/dividends/{ticker}")  # EQUITY-05
   @router.get("/fundamentals/{ticker}")  # EQUITY-06 (enhanced with ROE)
   @router.get("/short-interest/{ticker}")  # EQUITY-07
   @router.get("/insiders/{ticker}")  # EQUITY-08
   @router.get("/options/{ticker}")  # EQUITY-09
   @router.get("/news/{ticker}")  # EQUITY-10
   ```
   Each stub returns `{"status": "not_implemented"}` with 501 status. This gives tests something to assert against and later waves replace the stubs.

3. **backend/api/main.py**: Add two new router imports and registrations:
   - `from .routes.fx import router as fx_router`
   - `from .routes.equity import router as equity_router`
   - `app.include_router(fx_router)` after the existing ingest_router line
   - `app.include_router(equity_router)` after fx_router

4. **Test files** — create per 03-VALIDATION.md:

   **backend/tests/api/__init__.py**: Empty file.

   **backend/tests/api/test_fx.py**: Test `GET /api/fx/USD/GBP`:
   - `test_usdgbp_returns_rate` — mock the DB to return a known FXRate row, assert response has `rate`, `base`, `quote`, `timestamp` keys
   - `test_fx_not_found` — mock empty DB, assert 404 with `error` key

   **backend/tests/api/test_equity.py**: Test scaffolds for all equity endpoints. Each test calls the endpoint and asserts either 501 (stub) or correct shape once implemented. Tests per VALIDATION.md:
   - `test_earnings_dates` — GET `/api/equity/earnings/AAPL`, assert response shape
   - `test_dividend_dates` — GET `/api/equity/dividends/AAPL`
   - `test_fundamentals_shape` — assert keys pe_ratio, ev_ebitda, roe, debt_equity, market_cap
   - `test_short_interest` — assert structured response or US-only note for LSE
   - `test_lse_ticker` — GET endpoints with `LLOY.L`, assert no crash
   - `test_options_chain` — assert calls+puts structure
   - `test_news` — assert headline list structure
   Use `pytest.mark.skip(reason="Stub — implement in Wave 1/2/3")` on tests that hit 501 stubs. Remove skip as stubs are replaced.

   **backend/tests/analysis/__init__.py**: Empty file.

   **backend/tests/analysis/test_black_scholes.py**: Per VALIDATION.md:
   - `test_call_delta_range` — bs_greeks(S=100, K=100, T=0.25, r=0.05, sigma=0.2, 'call') delta must be in (0, 1)
   - `test_put_delta_range` — put delta must be in (-1, 0)
   - `test_vega_positive` — vega > 0 for both call and put
   - `test_theta_negative` — theta < 0 for call
   - `test_zero_time_returns_none` — T=0 returns all None
   - `test_atm_call_delta_near_0_5` — ATM call (S=K) delta should be approximately 0.5 (within 0.1)

   **backend/tests/analysis/test_insider.py**: Per VALIDATION.md:
   - `test_filter_10b5_1` — transaction with code "F" is excluded from counts
   - `test_buy_sell_ratio` — 3 buys + 1 sell = ratio 3.0
   - `test_multi_insider_detection` — 2 different names buying within 14 days = multi_insider True
   - `test_empty_input` — empty list returns zeros
  </action>
  <verify>
    <automated>cd C:/hhbfin/backend && python -m pytest tests/analysis/test_black_scholes.py tests/analysis/test_insider.py -x -q 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "get_fx_rate" backend/api/routes/fx.py` returns 1
    - `grep -c "fx_router" backend/api/main.py` returns at least 1
    - `grep -c "equity_router" backend/api/main.py` returns at least 1
    - `ls backend/tests/api/test_equity.py` succeeds
    - `ls backend/tests/api/test_fx.py` succeeds
    - `ls backend/tests/analysis/test_black_scholes.py` succeeds
    - `ls backend/tests/analysis/test_insider.py` succeeds
    - `pytest backend/tests/analysis/ -x -q` passes all tests
    - `grep "501" backend/api/routes/equity.py` shows stubs return 501
    - `grep "skip" backend/tests/api/test_equity.py` shows stub tests are skipped
  </acceptance_criteria>
  <done>FX endpoint returns rate from fx_rates table; equity route stubs registered in main.py; all 4 test files created per VALIDATION.md; Black-Scholes and insider analysis tests pass; equity endpoint tests are properly skipped pending implementation.</done>
</task>

</tasks>

<verification>
- `python -m pytest backend/tests/analysis/ -x -q` — all BS + insider tests pass
- `python -c "import scipy; print(scipy.__version__)"` — scipy importable
- `grep "interval" backend/models/ohlcv.py` — interval column exists
- `grep "roe" backend/models/fundamentals.py` — roe column exists
- `grep "fx_router" backend/api/main.py` — FX router registered
- `grep "equity_router" backend/api/main.py` — equity router registered
</verification>

<success_criteria>
- scipy in requirements.txt
- OHLCV model has interval column; Fundamentals model has roe column
- Alembic migration 0003 exists
- GET /api/fx/USD/GBP endpoint works (returns rate or 404)
- Black-Scholes returns correct Greeks for known inputs (all tests green)
- Insider clustering filters 10b5-1 codes correctly (all tests green)
- Finnhub REST source helpers exist
- All 4 test scaffold files created per VALIDATION.md
</success_criteria>

<output>
After completion, create `.planning/phases/03-equity-overview/03-01-SUMMARY.md`
</output>
