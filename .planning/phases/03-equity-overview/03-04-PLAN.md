---
phase: 03-equity-overview
plan: 04
type: execute
wave: 2
depends_on: ["03-02"]
files_modified:
  - backend/api/routes/equity.py
  - backend/tests/api/test_equity.py
  - frontend/src/components/equity/FundamentalsPanel.tsx
  - frontend/src/components/equity/ShortInterestPanel.tsx
  - frontend/src/components/equity/InsiderPanel.tsx
  - frontend/src/components/equity/NewsPanel.tsx
autonomous: true
requirements:
  - EQUITY-06
  - EQUITY-07
  - EQUITY-08
  - EQUITY-10

must_haves:
  truths:
    - "Fundamentals endpoint returns P/E, EV/EBITDA, ROE, Debt/Equity, Market Cap"
    - "Short interest endpoint returns data for US tickers or US-only message for LSE"
    - "Insider endpoint returns clustered buy/sell data with 10b5-1 filtered"
    - "Fundamentals panel renders 5 metrics with 24h cache badge"
    - "Short interest panel shows % float, short ratio, days to cover"
    - "Insider panel shows buy/sell ratio and multi-insider flag"
    - "News panel shows scrollable headlines with source and timestamp"
  artifacts:
    - path: "frontend/src/components/equity/FundamentalsPanel.tsx"
      provides: "Fundamentals data display panel"
      min_lines: 30
    - path: "frontend/src/components/equity/ShortInterestPanel.tsx"
      provides: "Short interest display"
      min_lines: 20
    - path: "frontend/src/components/equity/InsiderPanel.tsx"
      provides: "Insider transaction clustering display"
      min_lines: 30
    - path: "frontend/src/components/equity/NewsPanel.tsx"
      provides: "Company news feed"
      min_lines: 30
  key_links:
    - from: "backend/api/routes/equity.py"
      to: "backend/analysis/insider.py"
      via: "import cluster_insiders"
      pattern: "cluster_insiders"
    - from: "backend/api/routes/equity.py"
      to: "backend/ingestion/sources/finnhub_source.py"
      via: "import fetch_short_interest, fetch_insider_transactions"
      pattern: "fetch_short_interest"
---

<objective>
Implement the fundamentals, short interest, insider clustering, and news backend endpoints, then build the corresponding frontend panels for the right sidebar and bottom row.

Purpose: These panels fill the right sidebar (D-01) and bottom-right news feed (D-11). Fundamentals/short/insider are the core research data alongside the price charts.
Output: 4 backend endpoints fully implemented, 4 frontend panel components, tests updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-equity-overview/03-CONTEXT.md
@.planning/phases/03-equity-overview/03-RESEARCH.md
@.planning/phases/03-equity-overview/03-02-SUMMARY.md

@backend/api/routes/equity.py
@backend/api/routes/quote.py
@backend/analysis/insider.py
@backend/analysis/black_scholes.py
@backend/ingestion/sources/finnhub_source.py
@backend/models/fundamentals.py
@backend/tests/api/test_equity.py
@frontend/src/lib/theme.ts
@frontend/src/types/equity.ts

<interfaces>
From backend/analysis/insider.py (created in 03-01):
```python
def cluster_insiders(transactions: list[dict], window_days: int = 14) -> dict:
    # Returns: {"buy_count", "sell_count", "buy_sell_ratio", "clusters", "multi_insider"}
```

From backend/ingestion/sources/finnhub_source.py (created in 03-01):
```python
def fetch_short_interest(symbol: str, api_key: str) -> dict | None
def fetch_insider_transactions(symbol: str, api_key: str) -> list[dict]
def fetch_company_news(symbol: str, api_key: str, from_date: str, to_date: str) -> list[dict]
```

Finnhub short interest response (from Research):
```json
{"shortInterest": 12345678, "date": "2024-01-15", "sharesOutstanding": 15000000000}
```

Finnhub insider transactions response:
```json
{"data": [{"name": "...", "share": N, "change": N, "filingDate": "...", "transactionDate": "...", "transactionCode": "P|S|F|A|D", "transactionPrice": N, "isDerivative": bool}]}
```

From frontend/src/lib/theme.ts:
```typescript
export const TERMINAL = { BG, AMBER, GREEN, RED, DIM, BORDER } as const;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fundamentals + short interest + insider backend endpoints</name>
  <files>
    backend/api/routes/equity.py,
    backend/tests/api/test_equity.py
  </files>
  <read_first>
    backend/api/routes/equity.py,
    backend/api/routes/quote.py,
    backend/models/fundamentals.py,
    backend/analysis/insider.py,
    backend/ingestion/sources/finnhub_source.py,
    backend/tests/api/test_equity.py,
    .planning/phases/03-equity-overview/03-RESEARCH.md (lines 128-164 for Finnhub REST)
  </read_first>
  <action>
1. **backend/api/routes/equity.py**: Replace the 501 stubs for fundamentals, short-interest, and insiders:

   **`GET /api/equity/fundamentals/{ticker}` (EQUITY-06)**:
   - Query Fundamentals table for latest row by ticker (same pattern as quote.py)
   - Also fetch ROE from yfinance `yf.Ticker(ticker).info.get("returnOnEquity")` if not in DB
   - Return `{"ticker": ticker, "pe_ratio": float|null, "ev_ebitda": float|null, "roe": float|null, "debt_equity": float|null, "market_cap": int|null, "stale": bool, "source": "yfinance"}`
   - Cache with `cache_set(redis_client, f"fundamentals:{ticker}", response, "fundamentals")` — 24h TTL
   - Check cache first via `cache_get`

   **`GET /api/equity/short-interest/{ticker}` (EQUITY-07)**:
   - If ticker ends with `.L` or starts with `^`, return `{"ticker": ticker, "available": False, "message": "Short interest data available for US tickers only (Finnhub free tier limitation)"}`
   - Otherwise, call `fetch_short_interest(ticker, api_key)` from finnhub_source
   - If data returned: compute `pct_float = short_interest / shares_outstanding * 100` if both values present
   - Return `{"ticker": ticker, "available": True, "short_interest": int, "shares_outstanding": int, "pct_float": float, "date": str, "source": "finnhub"}`
   - If Finnhub returns None/error (403 on free tier): return `{"ticker": ticker, "available": False, "message": "Short interest unavailable on Finnhub free tier"}`
   - Cache with 1h TTL

   **`GET /api/equity/insiders/{ticker}` (EQUITY-08)**:
   - If ticker ends with `.L` or starts with `^`, return same US-only message pattern
   - Call `fetch_insider_transactions(ticker, api_key)`
   - Pass result to `cluster_insiders(transactions)` from analysis module
   - Return `{"ticker": ticker, "available": True, "buy_count": int, "sell_count": int, "buy_sell_ratio": float|null, "multi_insider": bool, "clusters": [...], "source": "finnhub"}`
   - Cache with 1h TTL

2. **backend/tests/api/test_equity.py**: Remove `@pytest.mark.skip` from:
   - `test_fundamentals_shape` — mock DB to return a Fundamentals row with pe_ratio=28.4, ev_ebitda=21.2, roe=0.45, debt_equity=1.73, market_cap=2940000000000. Assert response contains all 5 keys.
   - `test_short_interest` — mock `fetch_short_interest` to return `{"shortInterest": 100000, "sharesOutstanding": 1000000, "date": "2026-01-01"}`. Assert `pct_float` is approximately 10.0. Also test with `.L` ticker asserting `available: False`.
   - `test_lse_ticker` — test `/api/equity/short-interest/LLOY.L` returns `available: False`; test `/api/equity/insiders/LLOY.L` returns `available: False`
   - Keep `test_options_chain` skipped (Wave 3)
  </action>
  <verify>
    <automated>cd C:/hhbfin/backend && python -m pytest tests/api/test_equity.py -x -q -k "fundamentals or short_interest or lse or insider" 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "501" backend/api/routes/equity.py` returns exactly 1 (only options stub remains)
    - `grep "returnOnEquity" backend/api/routes/equity.py` returns at least 1 match
    - `grep "cluster_insiders" backend/api/routes/equity.py` returns at least 1 match
    - `grep "fetch_short_interest" backend/api/routes/equity.py` returns at least 1 match
    - `grep "US tickers only" backend/api/routes/equity.py` returns at least 1 match
    - `pytest backend/tests/api/test_equity.py -k "fundamentals" -x` passes
    - `pytest backend/tests/api/test_equity.py -k "short_interest" -x` passes
    - `pytest backend/tests/api/test_equity.py -k "lse" -x` passes
  </acceptance_criteria>
  <done>Fundamentals returns 5 metrics with ROE; short interest returns structured data or US-only message; insiders returns clustered data; tests pass for all 3 endpoints plus LSE edge case.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend panels — Fundamentals, Short Interest, Insider, News</name>
  <files>
    frontend/src/components/equity/FundamentalsPanel.tsx,
    frontend/src/components/equity/ShortInterestPanel.tsx,
    frontend/src/components/equity/InsiderPanel.tsx,
    frontend/src/components/equity/NewsPanel.tsx
  </files>
  <read_first>
    frontend/src/lib/theme.ts,
    frontend/src/types/equity.ts,
    .planning/phases/03-equity-overview/03-CONTEXT.md (D-01 layout zones, D-02 stale badge, D-11 news),
    .planning/phases/03-equity-overview/03-RESEARCH.md (lines 128-164 for data shapes)
  </read_first>
  <action>
1. **frontend/src/components/equity/FundamentalsPanel.tsx** (D-01 right sidebar, EQUITY-06):
   - Props: `{ ticker: string }`
   - Fetch from `GET /api/equity/fundamentals/{ticker}` on ticker change
   - Display as compact vertical list (right sidebar space is limited):
     ```
     FUNDAMENTALS            [24h cache badge]
     P/E           28.40
     EV/EBITDA     21.20
     ROE           45.00%
     Debt/Equity    1.73
     Market Cap    $2.94T
     ```
   - Format market_cap: use `$X.XXT` for trillions, `$X.XXB` for billions, `$X.XXM` for millions
   - ROE displayed as percentage (multiply by 100 if raw decimal)
   - If `stale: true` in response, show amber `[STALE]` badge per D-02
   - If data is null for a field, show `--`
   - Use `text-xs`, `text-terminal-amber` for labels, `text-terminal-green` for values, `border-terminal-border` dividers
   - Title row: `FUNDAMENTALS` left-aligned, stale badge right-aligned

2. **frontend/src/components/equity/ShortInterestPanel.tsx** (D-01 right sidebar, EQUITY-07):
   - Props: `{ ticker: string }`
   - Fetch from `GET /api/equity/short-interest/{ticker}`
   - If `available: false`, show compact message: `SHORT INTEREST [US ONLY]`
   - If available, display:
     ```
     SHORT INTEREST
     % Float Short   12.34%
     Short Interest   12.3M
     Date            2026-01-15
     ```
   - Same styling as FundamentalsPanel — `text-xs`, terminal colors

3. **frontend/src/components/equity/InsiderPanel.tsx** (D-01 right sidebar, EQUITY-08):
   - Props: `{ ticker: string }`
   - Fetch from `GET /api/equity/insiders/{ticker}`
   - If `available: false`, show `INSIDER ACTIVITY [US ONLY]`
   - If available, display as compact table:
     ```
     INSIDER ACTIVITY        [MULTI-BUY badge if multi_insider]
     Buys     12    Sells    3
     B/S Ratio 4.00
     Clusters  2 (last 30d)
     ```
   - If `multi_insider: true`, show amber `MULTI-BUY` badge — this is the key signal
   - Use `text-terminal-green` for buy counts, `text-terminal-red` for sell counts

4. **frontend/src/components/equity/NewsPanel.tsx** (D-11, EQUITY-10):
   - Props: `{ ticker: string }`
   - Fetch from `GET /api/equity/news/{ticker}`
   - Auto-refresh every 5 minutes (setInterval in useEffect)
   - Display as scrollable list within fixed panel height (this is the ONE scrollable panel per D-01 exception):
     ```
     NEWS
     [headline text]                              source  2h ago
     [headline text]                              source  5h ago
     ```
   - Each item: headline (truncated if too long), source badge, relative timestamp
   - Sentiment badge not available yet (Phase 7 FinBERT) — skip for now, add placeholder `[--]`
   - If `stale: true`, show amber `[STALE]` badge on panel header
   - `overflow-y-auto` on the list container, `max-h-full` to stay within panel bounds
   - Link each headline to its URL via `<a href={url} target="_blank">`
  </action>
  <verify>
    <automated>cd C:/hhbfin && ls frontend/src/components/equity/FundamentalsPanel.tsx frontend/src/components/equity/ShortInterestPanel.tsx frontend/src/components/equity/InsiderPanel.tsx frontend/src/components/equity/NewsPanel.tsx && grep "text-terminal-amber" frontend/src/components/equity/FundamentalsPanel.tsx && grep "US ONLY\|US tickers" frontend/src/components/equity/ShortInterestPanel.tsx && grep "MULTI-BUY\|multi_insider" frontend/src/components/equity/InsiderPanel.tsx && grep "overflow-y-auto" frontend/src/components/equity/NewsPanel.tsx</automated>
  </verify>
  <acceptance_criteria>
    - All 4 panel files exist in `frontend/src/components/equity/`
    - `grep "P/E\|pe_ratio" frontend/src/components/equity/FundamentalsPanel.tsx` returns at least 1 match
    - `grep "ROE\|roe" frontend/src/components/equity/FundamentalsPanel.tsx` returns at least 1 match
    - `grep "STALE\|stale" frontend/src/components/equity/FundamentalsPanel.tsx` returns at least 1 match
    - `grep "US ONLY" frontend/src/components/equity/ShortInterestPanel.tsx` returns at least 1 match
    - `grep "multi_insider\|MULTI" frontend/src/components/equity/InsiderPanel.tsx` returns at least 1 match
    - `grep "overflow-y-auto" frontend/src/components/equity/NewsPanel.tsx` returns at least 1 match
    - `grep "setInterval\|5.*min\|300000" frontend/src/components/equity/NewsPanel.tsx` returns at least 1 match (5m refresh)
    - `grep "text-xs" frontend/src/components/equity/FundamentalsPanel.tsx` returns at least 1 match (density)
  </acceptance_criteria>
  <done>4 frontend panels render with terminal styling; fundamentals shows 5 metrics; short interest and insider show US-only badge for LSE; news is scrollable with 5m refresh; all panels handle stale data.</done>
</task>

</tasks>

<verification>
- `pytest backend/tests/api/test_equity.py -x -q` — fundamentals, short interest, insider, LSE tests pass
- `ls frontend/src/components/equity/*.tsx | wc -l` — at least 4 panel files
- `grep "501" backend/api/routes/equity.py` — only options stub remains
</verification>

<success_criteria>
- Fundamentals endpoint returns P/E, EV/EBITDA, ROE, Debt/Equity, Market Cap with 24h cache
- Short interest returns data for US, US-only message for LSE (EQUITY-07, EQUITY-12)
- Insider clustering returns filtered/clustered data (EQUITY-08)
- 4 frontend panels render with terminal aesthetic and handle edge cases
- News panel auto-refreshes every 5 minutes (EQUITY-10)
</success_criteria>

<output>
After completion, create `.planning/phases/03-equity-overview/03-04-SUMMARY.md`
</output>
