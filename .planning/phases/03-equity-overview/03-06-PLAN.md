---
phase: 03-equity-overview
plan: 06
type: execute
wave: 3
depends_on: ["03-03", "03-04"]
files_modified:
  - frontend/src/components/equity/EquityModule.tsx
  - frontend/src/components/equity/QuoteBar.tsx
  - frontend/src/components/equity/TickerCommandBar.tsx
  - frontend/src/components/equity/GBPToggle.tsx
  - frontend/src/App.tsx
autonomous: false
requirements:
  - EQUITY-01
  - EQUITY-02
  - EQUITY-03
  - EQUITY-11
  - EQUITY-12

must_haves:
  truths:
    - "Typing a ticker in command bar loads all panels simultaneously"
    - "Live quote bar updates via WebSocket"
    - "Bloomberg fixed-height grid layout — nothing scrolls except news panel"
    - "GBP toggle converts prices using FX endpoint"
    - "LSE tickers (.L suffix) and indices (^FTSE) work in command bar"
    - "All panels visible simultaneously in the grid"
  artifacts:
    - path: "frontend/src/components/equity/EquityModule.tsx"
      provides: "Full equity module Bloomberg grid layout"
      min_lines: 80
    - path: "frontend/src/components/equity/QuoteBar.tsx"
      provides: "Live quote strip"
      min_lines: 30
    - path: "frontend/src/components/equity/TickerCommandBar.tsx"
      provides: "Terminal-style ticker input"
      min_lines: 20
    - path: "frontend/src/components/equity/GBPToggle.tsx"
      provides: "GBP/USD toggle button"
      min_lines: 15
  key_links:
    - from: "frontend/src/components/equity/EquityModule.tsx"
      to: "frontend/src/components/equity/ChartPanel.tsx"
      via: "React composition"
      pattern: "<ChartPanel"
    - from: "frontend/src/components/equity/EquityModule.tsx"
      to: "frontend/src/components/equity/OptionsChain.tsx"
      via: "React composition"
      pattern: "<OptionsChain"
    - from: "frontend/src/App.tsx"
      to: "frontend/src/components/equity/EquityModule.tsx"
      via: "conditional render activeTab === EQUITY"
      pattern: "EquityModule"
    - from: "frontend/src/components/equity/GBPToggle.tsx"
      to: "/api/fx/USD/GBP"
      via: "fetch"
      pattern: "fx/USD/GBP"
---

<objective>
Assemble the complete Equity module: Bloomberg grid layout, ticker command bar, live quote strip, GBP toggle, and wire all panels into App.tsx.

Purpose: This is the final assembly that delivers the full Bloomberg DES/GP equivalent. Every panel from previous waves is composed into the fixed-height grid layout (D-01). The ticker command bar (D-03) is the primary interaction point.
Output: EquityModule with all panels, QuoteBar, TickerCommandBar, GBPToggle, App.tsx wired.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-equity-overview/03-CONTEXT.md
@.planning/phases/03-equity-overview/03-03-SUMMARY.md
@.planning/phases/03-equity-overview/03-04-SUMMARY.md

@frontend/src/App.tsx
@frontend/src/lib/theme.ts
@frontend/src/types/equity.ts
@frontend/src/hooks/useKeyboard.ts
@frontend/src/hooks/useEquityWebSocket.ts
@frontend/src/hooks/useEquityData.ts
@frontend/src/components/equity/ChartPanel.tsx
@frontend/src/components/equity/FundamentalsPanel.tsx
@frontend/src/components/equity/ShortInterestPanel.tsx
@frontend/src/components/equity/InsiderPanel.tsx
@frontend/src/components/equity/NewsPanel.tsx
@frontend/src/components/equity/OptionsChain.tsx

<interfaces>
From frontend/src/App.tsx:
```typescript
// Current main content area (line 41-48):
<main className="p-2">
  <div className="text-terminal-green">[{activeTab}] MODULE READY</div>
</main>
// Replace with: {activeTab === "EQUITY" && <EquityModule />}
```

From frontend/src/hooks/useEquityWebSocket.ts:
```typescript
export function useEquityWebSocket(ticker: string): Quote | null
```

From frontend/src/hooks/useEquityData.ts:
```typescript
export function useEquityData(ticker: string): {
  chartData: Record<Timeframe, OHLCVBar[]>,
  earningsMarkers: ChartMarker[],
  dividendMarkers: ChartMarker[],
}
```

From frontend/src/types/equity.ts:
```typescript
export interface Quote { ticker, price, change_pct, open, high, low, close, volume, stale, timestamp }
```

From 03-CONTEXT.md D-01 Layout:
- Top strip (full width): Live quote bar
- Command bar: above the grid, TICKER> ____ with amber cursor
- Left panel (~60%): 4-panel chart view
- Right sidebar (~40%): Fundamentals -> Short interest -> Insider clustering
- Bottom row: Options chain (~60%) + News feed (~40%)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: TickerCommandBar + QuoteBar + GBPToggle components</name>
  <files>
    frontend/src/components/equity/TickerCommandBar.tsx,
    frontend/src/components/equity/QuoteBar.tsx,
    frontend/src/components/equity/GBPToggle.tsx
  </files>
  <read_first>
    frontend/src/lib/theme.ts,
    frontend/src/types/equity.ts,
    frontend/src/hooks/useEquityWebSocket.ts,
    .planning/phases/03-equity-overview/03-CONTEXT.md (D-03, D-04, D-12),
    docs/superpowers/specs/2026-03-24-bloomberg-terminal-design.md (first 100 lines for terminal aesthetic)
  </read_first>
  <action>
1. **frontend/src/components/equity/TickerCommandBar.tsx** (D-03, D-04):
   - Props: `{ onSubmit: (ticker: string) => void }`
   - Render terminal-style input: `TICKER> ` prefix in amber, input field with amber text, black background, monospace font, blinking cursor
   - Input: `<input>` with `bg-transparent border-none outline-none text-terminal-amber font-terminal text-xs uppercase` classes
   - Auto-uppercase: `onChange={(e) => setValue(e.target.value.toUpperCase())}`
   - On Enter: call `onSubmit(value.trim())`, clear input
   - The `TICKER> ` prefix is a `<span>` not part of the input
   - Cursor effect: CSS `caret-color: #ff9900` (amber)
   - Full width, single row height, `border-b border-terminal-border`
   - Auto-focus on mount via `useEffect(() => inputRef.current?.focus(), [])`
   - Per D-04: no history, no autocomplete

2. **frontend/src/components/equity/QuoteBar.tsx** (D-01 top strip, EQUITY-01):
   - Props: `{ quote: Quote | null, gbpMode: boolean, gbpRate: number | null }`
   - Display in a single row (full width, compact):
     ```
     AAPL  189.50  +0.65%  O:187.20 H:190.10 L:186.80  Vol:42.8M  [stale badge] [GBP toggle]
     ```
   - Ticker name: `text-terminal-amber font-bold`
   - Price: `text-terminal-green` if positive change, `text-terminal-red` if negative
   - Change %: same color logic, prefix with `+` or `-`
   - O/H/L: `text-terminal-dim`
   - Volume: formatted with K/M/B suffix
   - If `quote.stale === true`: show amber `STALE` badge per D-02
   - If `gbpMode` and `gbpRate`: multiply all price fields by gbpRate, show `GBP` label
   - If no quote yet: show `--` placeholders
   - `border-b border-terminal-border py-0.5 px-2`

3. **frontend/src/components/equity/GBPToggle.tsx** (D-12, EQUITY-11):
   - Props: `{ active: boolean, onToggle: () => void }`
   - Render button: `GBP` text
   - When active: `bg-terminal-amber text-black font-bold` (highlighted)
   - When inactive: `text-terminal-dim border border-terminal-border`
   - Size: compact, `text-xs px-2 py-0.5`
   - Placed inside QuoteBar (passed as child or composed)
  </action>
  <verify>
    <automated>cd C:/hhbfin && ls frontend/src/components/equity/TickerCommandBar.tsx frontend/src/components/equity/QuoteBar.tsx frontend/src/components/equity/GBPToggle.tsx && grep "TICKER>" frontend/src/components/equity/TickerCommandBar.tsx && grep "toUpperCase" frontend/src/components/equity/TickerCommandBar.tsx && grep "STALE\|stale" frontend/src/components/equity/QuoteBar.tsx && grep "GBP" frontend/src/components/equity/GBPToggle.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `ls frontend/src/components/equity/TickerCommandBar.tsx` succeeds
    - `grep "TICKER>" frontend/src/components/equity/TickerCommandBar.tsx` returns at least 1 match
    - `grep "toUpperCase" frontend/src/components/equity/TickerCommandBar.tsx` returns at least 1 match (D-03 auto-uppercase)
    - `grep "onSubmit" frontend/src/components/equity/TickerCommandBar.tsx` returns at least 1 match
    - `grep "caret-color\|caretColor" frontend/src/components/equity/TickerCommandBar.tsx` returns at least 1 match (amber cursor)
    - `grep "stale\|STALE" frontend/src/components/equity/QuoteBar.tsx` returns at least 1 match (D-02)
    - `grep "gbpRate\|gbpMode" frontend/src/components/equity/QuoteBar.tsx` returns at least 1 match (D-12)
    - `grep "GBP" frontend/src/components/equity/GBPToggle.tsx` returns at least 1 match
  </acceptance_criteria>
  <done>TickerCommandBar has terminal-style TICKER> input with amber cursor and auto-uppercase; QuoteBar shows live data with stale badge; GBPToggle toggles GBP price mode.</done>
</task>

<task type="auto">
  <name>Task 2: EquityModule assembly + App.tsx wiring</name>
  <files>
    frontend/src/components/equity/EquityModule.tsx,
    frontend/src/App.tsx
  </files>
  <read_first>
    frontend/src/App.tsx,
    frontend/src/components/equity/TickerCommandBar.tsx,
    frontend/src/components/equity/QuoteBar.tsx,
    frontend/src/components/equity/GBPToggle.tsx,
    frontend/src/components/equity/ChartPanel.tsx,
    frontend/src/components/equity/FundamentalsPanel.tsx,
    frontend/src/components/equity/ShortInterestPanel.tsx,
    frontend/src/components/equity/InsiderPanel.tsx,
    frontend/src/components/equity/OptionsChain.tsx,
    frontend/src/components/equity/NewsPanel.tsx,
    frontend/src/hooks/useEquityData.ts,
    frontend/src/hooks/useEquityWebSocket.ts,
    .planning/phases/03-equity-overview/03-CONTEXT.md (D-01 full layout)
  </read_first>
  <action>
1. **frontend/src/components/equity/EquityModule.tsx**: The master layout component (D-01):
   ```typescript
   export default function EquityModule() {
     const [ticker, setTicker] = useState<string>('');
     const [gbpMode, setGbpMode] = useState(false);
     const [gbpRate, setGbpRate] = useState<number | null>(null);
     const quote = useEquityWebSocket(ticker);
     const { chartData, earningsMarkers, dividendMarkers } = useEquityData(ticker);
   ```

   - On ticker submit from TickerCommandBar: `setTicker(newTicker)` — this triggers all hooks to refetch (D-04: all panels refresh simultaneously)
   - Fetch GBP rate when gbpMode toggled on: `fetch('/api/fx/USD/GBP')` then `setGbpRate(data.rate)` (EQUITY-11)

   - **Layout using CSS Grid** (D-01 Bloomberg fixed-height, nothing scrolls):
     ```
     h-[calc(100vh-HEADER-TAB-FOOTER)]  // Fill remaining viewport height
     grid grid-rows-[auto_auto_1fr_minmax(200px,25vh)]
     ```
     Row 1: TickerCommandBar (auto height, ~30px)
     Row 2: QuoteBar with GBPToggle (auto height, ~24px)
     Row 3: Main content area (flex-1 to fill space):
       - CSS Grid: `grid grid-cols-[60%_40%] h-full`
       - Left col: ChartPanel (4-panel charts)
       - Right col: `flex flex-col` stacking FundamentalsPanel, ShortInterestPanel, InsiderPanel (each taking equal space with `flex-1 overflow-hidden`)
     Row 4: Bottom row (~25vh or 200px min):
       - CSS Grid: `grid grid-cols-[60%_40%] h-full`
       - Left: OptionsChain (with IVSurface above it)
       - Right: NewsPanel (scrollable per D-11 exception)

   - All panels wrapped in `border border-terminal-border` containers
   - When no ticker entered, show centered message: `TYPE A TICKER TO BEGIN` in `text-terminal-dim`
   - Pass `ticker` to every panel as prop

2. **frontend/src/App.tsx**: Replace the placeholder main content:
   - Add import: `import EquityModule from './components/equity/EquityModule'`
   - Replace the `<main>` content (lines 41-48) with:
     ```tsx
     <main className="flex-1 overflow-hidden">
       {activeTab === "EQUITY" && <EquityModule />}
       {activeTab !== "EQUITY" && (
         <div className="p-2">
           <div className="text-terminal-green">[{activeTab}] MODULE READY</div>
           <p className="text-terminal-dim mt-2">Press 1-6 to switch modules.</p>
         </div>
       )}
     </main>
     ```
   - Change the outer div from `min-h-screen` to `h-screen flex flex-col` — the Bloomberg layout requires fixed viewport height (D-01: nothing scrolls)
   - The `<main>` gets `flex-1 overflow-hidden` to fill remaining space after header/nav/footer
  </action>
  <verify>
    <automated>cd C:/hhbfin && ls frontend/src/components/equity/EquityModule.tsx && grep "EquityModule" frontend/src/App.tsx && grep "activeTab.*EQUITY" frontend/src/App.tsx && grep "grid-cols-\[60%.*40%\]\|grid-cols" frontend/src/components/equity/EquityModule.tsx && grep "ChartPanel" frontend/src/components/equity/EquityModule.tsx && grep "OptionsChain" frontend/src/components/equity/EquityModule.tsx && grep "h-screen\|100vh" frontend/src/App.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `ls frontend/src/components/equity/EquityModule.tsx` succeeds
    - `grep "EquityModule" frontend/src/App.tsx` returns at least 1 match (import or usage)
    - `grep "activeTab.*EQUITY" frontend/src/App.tsx` returns at least 1 match
    - `grep "ChartPanel" frontend/src/components/equity/EquityModule.tsx` returns at least 1 match
    - `grep "FundamentalsPanel" frontend/src/components/equity/EquityModule.tsx` returns at least 1 match
    - `grep "OptionsChain" frontend/src/components/equity/EquityModule.tsx` returns at least 1 match
    - `grep "NewsPanel" frontend/src/components/equity/EquityModule.tsx` returns at least 1 match
    - `grep "GBPToggle\|gbpMode" frontend/src/components/equity/EquityModule.tsx` returns at least 1 match
    - `grep "TickerCommandBar" frontend/src/components/equity/EquityModule.tsx` returns at least 1 match
    - `grep "h-screen\|100vh" frontend/src/App.tsx` returns at least 1 match (fixed viewport)
    - `grep "flex-1\|overflow-hidden" frontend/src/App.tsx` returns at least 1 match
  </acceptance_criteria>
  <done>EquityModule assembles all panels in Bloomberg grid layout; App.tsx renders it when EQUITY tab active; viewport fixed-height (nothing scrolls except news); ticker input loads all panels.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visual verification of Equity module</name>
  <files>frontend/src/components/equity/EquityModule.tsx</files>
  <action>
    User visually verifies the complete Equity Overview module in the browser. All automated work is complete in Tasks 1-2 — this checkpoint confirms the visual layout matches the Bloomberg terminal aesthetic specified in D-01 through D-13.
  </action>
  <verify>
    <automated>cd C:/hhbfin/frontend && npx tsc --noEmit 2>&1 | tail -3</automated>
  </verify>
  <done>User has visually approved the Equity module layout including: 4-panel charts, fundamentals sidebar, options chain, news feed, ticker command bar, GBP toggle, and LSE ticker support.</done>
  <what-built>Complete Equity Overview module with Bloomberg grid layout, ticker command bar, live quote strip, 4-panel charts, fundamentals/short-interest/insider sidebar, options chain with IV heatmap, and news feed.</what-built>
  <how-to-verify>
    1. Start services: `docker compose up -d` from C:/hhbfin
    2. Open browser at http://localhost:3000
    3. Verify EQUITY tab is active by default
    4. Type `AAPL` in the command bar and press Enter
    5. Verify: Quote bar shows price, change %, volume
    6. Verify: 4 chart panels show Weekly/Daily/4H/1H candlesticks
    7. Click one chart to expand — verify it fills the chart zone
    8. Press Escape — verify it collapses back to 4-panel view
    9. Verify: Right sidebar shows Fundamentals (P/E, EV/EBITDA, ROE, D/E, MCap)
    10. Verify: Short interest panel shows data (or US-only badge if Finnhub blocks free tier)
    11. Verify: Insider panel shows buy/sell data
    12. Verify: Options chain table shows calls left, strike centre, puts right with Greeks columns
    13. Verify: IV surface heatmap renders above options table
    14. Verify: News panel shows headlines (scrollable)
    15. Click GBP toggle — verify prices change to GBP values
    16. Type `LLOY.L` — verify: charts load, fundamentals show, short interest shows [US ONLY], options show [NOT AVAILABLE]
    17. Verify: Overall layout is dense Bloomberg terminal style — no scrolling on main page
  </how-to-verify>
  <resume-signal>Type "approved" or describe specific issues to fix</resume-signal>
</task>

</tasks>

<verification>
- `cd frontend && npx tsc --noEmit` — TypeScript compiles
- `cd frontend && npm run build` — production build succeeds
- `pytest backend/tests/ -x -q` — all backend tests pass
- Visual verification by user (Task 3 checkpoint)
</verification>

<success_criteria>
- EquityModule renders complete Bloomberg grid layout per D-01
- Ticker command bar works with AAPL, LLOY.L, ^FTSE (D-03, D-13)
- Quote bar shows live data from WebSocket (EQUITY-01)
- GBP toggle converts prices (EQUITY-11)
- All panels visible simultaneously (D-01)
- User visually approves the layout
</success_criteria>

<output>
After completion, create `.planning/phases/03-equity-overview/03-06-SUMMARY.md`
</output>
