---
phase: 02-data-ingestion-foundation
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - backend/cache/ttl.py
  - backend/cache/rate_limiter.py
  - backend/cache/__init__.py
  - backend/ingestion/config.py
  - backend/tests/ingestion/test_cache.py
autonomous: true
requirements:
  - INGEST-03
  - INGEST-04

must_haves:
  truths:
    - "Redis TTL caching sets correct expiry per data type from spec section 5"
    - "Token bucket rate limiter atomically checks and decrements tokens via Lua script"
    - "Rate limit constants match spec: Finnhub 60/min, FMP 250/day, Alpha Vantage 25/day, CoinGecko 13/hr"
  artifacts:
    - path: "backend/cache/ttl.py"
      provides: "TTL constants + cache_set/cache_get helpers"
      exports: ["TTL", "cache_set", "cache_get"]
    - path: "backend/cache/rate_limiter.py"
      provides: "Lua-based atomic token bucket rate limiter"
      exports: ["check_rate_limit", "RATE_LIMITS"]
    - path: "backend/ingestion/config.py"
      provides: "Seed ticker list, FRED series mapping, schedule intervals"
      exports: ["SEED_TICKERS", "FRED_SERIES_MAP", "RETRY_COUNTDOWNS"]
  key_links:
    - from: "backend/cache/rate_limiter.py"
      to: "Redis"
      via: "EVAL Lua script"
      pattern: "redis_client\\.eval"
---

<objective>
Build the Redis caching layer (TTL helper + token bucket rate limiter) and shared ingestion configuration.

Purpose: All ingestion workers (Plans 03-05) depend on the cache layer for TTL management and rate limiting. Config provides shared constants (seed tickers, FRED mappings, retry delays).
Output: cache/ttl.py, cache/rate_limiter.py, ingestion/config.py, test_cache.py
</objective>

<execution_context>
@C:/Users/Surface-Pro-1/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/Surface-Pro-1/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-data-ingestion-foundation/02-CONTEXT.md
@.planning/phases/02-data-ingestion-foundation/02-RESEARCH.md

<interfaces>
From backend/api/redis_client.py (existing sync client):
```python
import redis
import os
REDIS_URL = os.environ.get("REDIS_URL", "redis://redis:6379/0")
redis_client = redis.Redis.from_url(REDIS_URL, decode_responses=True)
def get_redis():
    return redis_client
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Redis TTL cache helper + token bucket rate limiter + config</name>
  <files>
    backend/cache/ttl.py,
    backend/cache/rate_limiter.py,
    backend/cache/__init__.py,
    backend/ingestion/config.py,
    backend/tests/ingestion/test_cache.py
  </files>
  <read_first>
    backend/cache/__init__.py,
    backend/api/redis_client.py
  </read_first>
  <behavior>
    - test_ttl_constants_exist: TTL dict contains keys "quote" (15), "fx" (30), "fundamentals" (86400), "macro" (3600), "yield_curve" (900)
    - test_cache_set_calls_redis_set_with_ex: cache_set calls redis.set(key, json_data, ex=TTL[ttl_key])
    - test_cache_get_returns_dict_on_hit: cache_get returns parsed JSON dict when redis.get returns data
    - test_cache_get_returns_none_on_miss: cache_get returns None when redis.get returns None
    - test_rate_limit_allowed: check_rate_limit returns True when Lua script returns 1
    - test_rate_limit_denied: check_rate_limit returns False when Lua script returns 0
    - test_rate_limits_config: RATE_LIMITS contains finnhub (capacity=60, per_seconds=60), fmp (capacity=250, per_seconds=86400), alpha_vantage (capacity=25, per_seconds=86400), coingecko (capacity=13, per_seconds=3600)
    - test_seed_tickers: SEED_TICKERS contains at minimum AAPL, MSFT, LLOY.L, BARC.L, ^FTSE, BTC-USD, GBP=X
    - test_fred_series_map: FRED_SERIES_MAP maps "cpi" to "CPIAUCSL", "gdp" to "GDP", "fed_funds" to "FEDFUNDS", "unemployment" to "UNRATE"
    - test_retry_countdowns: RETRY_COUNTDOWNS equals [60, 300, 900] per D-02
  </behavior>
  <action>
1. **backend/cache/__init__.py** — update to export cache helpers:
   ```python
   from .ttl import TTL, cache_set, cache_get
   from .rate_limiter import check_rate_limit, RATE_LIMITS
   ```

2. **backend/cache/ttl.py** — TTL constants from spec section 5 (per D-05) + JSON cache helpers:
   ```python
   import json
   from typing import Any, Optional

   # TTL values from spec section 5 — authoritative source (per D-05)
   TTL = {
       "quote": 15,           # live quotes 15s
       "fx": 30,              # FX rates 30s
       "fundamentals": 86400, # 24h
       "macro": 3600,         # FRED 1h
       "yield_curve": 900,    # 15m
       "news": 300,           # 5m
       "screener": 900,       # 15m
       "crypto_marketcap": 600, # 10m
   }

   def cache_set(redis_client, key: str, data: Any, ttl_key: str) -> None:
       redis_client.set(key, json.dumps(data, default=str), ex=TTL[ttl_key])

   def cache_get(redis_client, key: str) -> Optional[dict]:
       raw = redis_client.get(key)
       return json.loads(raw) if raw else None
   ```

3. **backend/cache/rate_limiter.py** — Lua-based atomic token bucket (per D-06):
   ```python
   import time

   TOKEN_BUCKET_LUA = """
   local key = KEYS[1]
   local capacity = tonumber(ARGV[1])
   local refill_rate = tonumber(ARGV[2])
   local now = tonumber(ARGV[3])

   local bucket = redis.call('HMGET', key, 'tokens', 'last_refill')
   local tokens = tonumber(bucket[1]) or capacity
   local last_refill = tonumber(bucket[2]) or now

   local elapsed = now - last_refill
   local refilled = math.min(capacity, tokens + elapsed * refill_rate)

   if refilled >= 1 then
       redis.call('HMSET', key, 'tokens', refilled - 1, 'last_refill', now)
       redis.call('EXPIRE', key, 3600)
       return 1
   else
       return 0
   end
   """

   # Rate limits per D-06: source -> (capacity, refill window in seconds)
   RATE_LIMITS = {
       "finnhub":       {"capacity": 60,  "per_seconds": 60},      # 60/min
       "fmp":           {"capacity": 250, "per_seconds": 86400},    # 250/day
       "alpha_vantage": {"capacity": 25,  "per_seconds": 86400},    # 25/day
       "coingecko":     {"capacity": 13,  "per_seconds": 3600},     # 13/hr
   }

   def check_rate_limit(redis_client, source: str) -> bool:
       """Returns True if request is allowed, False if rate limited."""
       if source not in RATE_LIMITS:
           return True  # No limit configured for this source
       limits = RATE_LIMITS[source]
       refill_rate = limits["capacity"] / limits["per_seconds"]
       result = redis_client.eval(
           TOKEN_BUCKET_LUA, 1, f"ratelimit:{source}",
           limits["capacity"], refill_rate, time.time()
       )
       return bool(result)
   ```

4. **backend/ingestion/config.py** — shared constants (per D-02, D-03, D-04, D-14):
   ```python
   # Seed tickers for scheduled ingestion (per D-04)
   SEED_TICKERS = [
       "AAPL", "MSFT", "LLOY.L", "BARC.L",
       "^FTSE", "^FTMC", "BTC-USD", "GBP=X", "EURUSD=X",
   ]

   # FRED series friendly name -> FRED ID mapping (per D-14)
   FRED_SERIES_MAP = {
       "cpi": "CPIAUCSL",
       "core_cpi": "CPILFESL",
       "pce": "PCEPI",
       "gdp": "GDP",
       "fed_funds": "FEDFUNDS",
       "unemployment": "UNRATE",
       "treasury_10y": "GS10",
       "treasury_2y": "GS2",
   }

   # Retry countdowns per D-02: 3 attempts at 60s / 300s / 900s
   RETRY_COUNTDOWNS = [60, 300, 900]

   # Schedule intervals (seconds)
   SCHEDULE_OHLCV = 300       # 5 min per D-03
   SCHEDULE_MACRO = 3600      # 1h
   SCHEDULE_FX = 30           # 30s per INGEST-07
   SCHEDULE_TREASURY = 900    # 15m per INGEST-08
   ```

5. **backend/tests/ingestion/test_cache.py** — write tests FIRST, then verify implementations pass:
   - Test all TTL constants match spec values
   - Test cache_set/cache_get with mock redis
   - Test rate limiter allowed/denied based on Lua return value
   - Test RATE_LIMITS dict contents
   - Test config constants (SEED_TICKERS, FRED_SERIES_MAP, RETRY_COUNTDOWNS)
  </action>
  <verify>
    <automated>cd C:/hhbfin && python -m pytest backend/tests/ingestion/test_cache.py -x -q</automated>
  </verify>
  <acceptance_criteria>
    - backend/cache/ttl.py contains `"quote": 15`
    - backend/cache/ttl.py contains `"fx": 30`
    - backend/cache/ttl.py contains `"fundamentals": 86400`
    - backend/cache/ttl.py contains `"macro": 3600`
    - backend/cache/ttl.py contains `"yield_curve": 900`
    - backend/cache/ttl.py contains `def cache_set(`
    - backend/cache/ttl.py contains `def cache_get(`
    - backend/cache/rate_limiter.py contains `TOKEN_BUCKET_LUA`
    - backend/cache/rate_limiter.py contains `def check_rate_limit(`
    - backend/cache/rate_limiter.py contains `"finnhub"` with capacity 60
    - backend/cache/rate_limiter.py contains `"fmp"` with capacity 250
    - backend/ingestion/config.py contains `SEED_TICKERS`
    - backend/ingestion/config.py contains `"AAPL"`
    - backend/ingestion/config.py contains `"LLOY.L"`
    - backend/ingestion/config.py contains `FRED_SERIES_MAP`
    - backend/ingestion/config.py contains `"CPIAUCSL"`
    - backend/ingestion/config.py contains `RETRY_COUNTDOWNS = [60, 300, 900]`
    - pytest test_cache.py exits 0
  </acceptance_criteria>
  <done>TTL cache helper sets correct per-type expiry. Token bucket rate limiter uses Lua for atomic check. Config provides seed tickers, FRED mapping, retry delays. All cache tests green.</done>
</task>

</tasks>

<verification>
- `python -m pytest backend/tests/ingestion/test_cache.py -x -q` passes
- TTL dict has all 8 entries matching spec section 5 values
- RATE_LIMITS dict has all 4 API sources with correct capacity/window
- SEED_TICKERS list covers US, UK, index, crypto, FX tickers
</verification>

<success_criteria>
- Redis TTL cache helper functional with correct per-type TTLs
- Lua-based token bucket rate limiter functional
- Ingestion config has all shared constants
- All cache tests pass
</success_criteria>

<output>
After completion, create `.planning/phases/02-data-ingestion-foundation/02-02-SUMMARY.md`
</output>
